/**
 * Sign-in Template - Theme Configuration
 * 
 * Clean, professional theme for authentication
 */

import type { ThemeConfig } from '../../types/theme';

export const signInTemplateTheme: ThemeConfig = {
  mode: 'light',
  primaryColor: '#1976d2',
  secondaryColor: '#424242',
  fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif',
  borderRadius: 8,
  spacing: 8,
};

// Full preset ID for rich theming
export const signInTemplateThemePresetId = 'auth-mui';
